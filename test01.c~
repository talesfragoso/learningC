#include <stdio.h>

int main(void)
{
	short int i = 0;
	char name[] = "Tales";
	int numbers[] = {84, 97, 108, 101, 115};

	while(i < 5) 
	{
		printf("%c: %d\n", name[i], name[i]);
		i++;
	}
	i = 0;	
	do
	{
		printf("%c", numbers[i]);
		i++;
	} while(i < 5);		
	return 0;
}
